import { Box } from '@mui/material';
import CardUser from '../../../shared/CardUser';
const Suggestion = () => {
    return (
        <Box>
            <CardUser />
        </Box>
    );
};

export default Suggestion;
